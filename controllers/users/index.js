const { CustomerLogin } = require('./login');
const { CustomerRegister } = require('./register');
const { UserInfo } = require('./user-info');

module.exports = { CustomerLogin, CustomerRegister, UserInfo };
